<div class="container">
  <div class="container text-center", style="margin-top:30px">
    <h3>Pour quelle boutique voulez-vous créer une offre ?</h3>
  </div>
    <div class="row-forms">
      <div class="column-forms">
        <div class="form-login">
        <%= simple_form_for (@shops[0]) do |f| %>
          <%= f.input :name, label: "Selectionnez votre boutique:", input_html: { class: 'select2' }, collection: @shops %>
          <%= f.submit 'Valider' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
